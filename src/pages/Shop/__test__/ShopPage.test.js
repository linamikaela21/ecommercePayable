// import { ShopPage } from '../ShopPage';
// import { render, screen } from '@testing-library/react';
// import { Route } from 'react-router-dom';

// jest.mock('react-redux', () => ({
//   useDispatch: () => jest.fn(),
//   useSelector: () => ({ isFetching: true }),
// }));

// // const match = { path: '/shop' };

// jest.mock('react-router-dom', () => ({
//   useParams: () => ({ collectionId: 'mens' }),
// }));

// jest.mock('react', () => ({
//   useEffect: () => jest.fn(),
// }));

describe('ShopPage', () => {
  // it('should render a ShopPage', () => {
  //   render(
  //     <Route>
  //       <ShopPage />
  //     </Route>
  //   );
  //   const shopPageElement = screen.getByTestId('test-id-ShopPage');
  //   expect(shopPageElement).toBeInTheDocument();
  // });
  it('DELETE', () => {
    expect(true).toBe(true);
  });
});
